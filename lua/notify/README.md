# 使用

```lua
message = string or table
options = {
        position = "top"
        row = , -- default -1
        col = , -- default win_current_width
        border = , -- default single
        style = , -- default info
        delay = , -- default 2000
    }
require("notificaton")(message, options)
```
